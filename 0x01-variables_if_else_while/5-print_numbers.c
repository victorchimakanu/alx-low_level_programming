#include <stdio.h>

/**
 * main - prints all single digit numbers of base 10
 * return: 0
 */

int main(void)
{
	int n = 0;

	while (n < 10)
	{
			
		printf("%i", n);
		n++;
	}
	putchar('\n');
	
	return (0);
}
